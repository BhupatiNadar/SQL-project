# SQL Project

This project contains SQL queries and database operations that demonstrate various concepts such as table creation, data insertion, constraints, joins, aggregate functions, subqueries, and more.

## 📁 Project Structure
```
├── EcommerceDB.sql
├── List all products along with their category names.docx
└── MiniProject.pdf
```


- `create_tables.sql` → Contains SQL commands to create database tables.
- `insert_data.sql` → Contains SQL queries for inserting sample data.
- `queries.sql` → Contains practice and task-based SQL queries.

## 🛠️ Requirements

To run this project, you need:

- **MySQL** / **MariaDB** / **PostgreSQL** / **SQL Server** (any one is fine)
- SQL client tool like:
  - MySQL Workbench
  - DBeaver
  - SQL Server Management Studio
  - or Terminal based SQL access

## 🚀 How to Run

1. Clone the repository:
   ```bash
   git clone https://github.com/BhupatiNadar/SQL-project.git
